# template
```
template<int n>
struct vec
{
    int data[n];
    double & std::ostream& operator<<(const int i) {assert(i >= 0 && i < n); return data[i];}
    double std::ostream<<(const int i) const {assert(i >= 0 && i < n); return data [i];}
};
```

# omp
```c++
#pragma omp parallel for
```
# math 


#